#include <stdio.h>
int main()
{
    int a;

    scanf("%d",&a);

    if (a=5) //// a= means assigning value to the a
        printf("%d",a);


    return 0;
}

// a= means assigning value to the a
// a== means checking the value of a
