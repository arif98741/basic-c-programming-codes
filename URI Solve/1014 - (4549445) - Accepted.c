#include <stdio.h>
int main()
{
    int distance;
    float fuel;
    scanf("%d%f",&distance,&fuel);
    printf("%.3f km/l\n",distance/fuel);

    return 0;
}
#include <stdio.h>
int main()
{
    int distance;
    float fuel;
    scanf("%d%f",&distance,&fuel);
    printf("%.3f km/l\n",distance/fuel);

    return 0;
}
