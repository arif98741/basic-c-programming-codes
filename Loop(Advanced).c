#include <stdio.h>
int main()
{
    int a,b,sum;

    while(scanf("%d %d",&a,&b)!=EOF) ///Ctrl+Z for ending the program execusion
        {
    sum=a+b;
    printf("%d\n",sum);

    }

    return 0;
}
